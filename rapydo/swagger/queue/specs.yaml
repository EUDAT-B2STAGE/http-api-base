
depends_on:
  # TO FIX: it should depend on the name of the service
  # as the name proposed in services.yaml
  - CELERY_AVAILABLE
class: Queue
file: endpoints
mapping:
  list_queue: "/queue"
  single_queue: "/queue/<task_id>"
ids:
  task_id: uuid referring to the task you are selecting
labels:
  - base
  - tasks
